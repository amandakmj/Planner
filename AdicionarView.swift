import SwiftUI

struct AdicionarView: View {
    var body: some View {
        NavigationView {
            Text("Tela para adicionar conteúdos futuramente ✨")
                .font(.body)
                .padding()
                .navigationTitle("Adicionar")
        }
    }
}
